## Run Alembic Migrations

### Configuration

```bash
cp alembic.ini.example alembic.ini


That will give you a clean section in your README showing how to configure Alembic by copying the example file and then editing it with your PostgreSQL connection string.  

Would you like me to also extend this README section with the **common Alembic commands** (like `alembic upgrade head`, `alembic revision --autogenerate`) so your teammates have a quick reference guide right inside the file?